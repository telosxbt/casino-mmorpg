// Character sheet frame size (set by tools/process-sprites.mjs — keep in sync).
// Cosmetic recolor presets were removed from the UX (revisit later); the backend
// columns + recolor tooling remain dormant.
export const FRAME = { w: 410, h: 326 };
